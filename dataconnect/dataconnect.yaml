specVersion: "v1"
serviceId: "notification"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "notification-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
